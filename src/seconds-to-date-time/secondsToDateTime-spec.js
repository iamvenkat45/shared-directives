describe('secondsToDateTime', function () {

    beforeEach(module('rmsv2.commonComponents'));

    it('should ...', inject(function ($filter) {

        var filter = $filter('secondsToDateTime');

        expect(filter('input')).toEqual('output');

    }));

});
